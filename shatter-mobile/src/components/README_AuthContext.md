# Components

This folder contains reusable UI components used across the application.

## Structure

Common examples include:

- Buttons
- Cards
- Layout wrappers
- Modals
- Form inputs

Larger feature-specific components can live in subfolders.

Example:
- `general/Button.tsx`
- `general/Card.tsx`
- `new-events/QRScannerBox.tsx`